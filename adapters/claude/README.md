# Claude Code Adapter

Claude Code is the production adapter. `nova-plugin/` contains its marketplace metadata, direct command adapters, compatibility skills, hooks, permissions, agents, and packs.

The generated `manifest.json` points to the install and live route gates. L4 is a release claim only when the exact-tag isolated install and route smoke succeed; local static validation alone is not equivalent.
