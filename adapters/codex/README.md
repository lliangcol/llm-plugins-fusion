# Codex Adapter

`AGENTS.md` is generated from the canonical workflow specification and supplies inventory, input, risk, and output-contract context. Codex consumes the referenced Markdown contracts directly and must not claim Claude hook or permission enforcement.

Static generation and conformance establish L3 adapter structure. Upgrade an exact Codex version to L4 only with a public-safe live record under `evals/evidence/` proving enumeration, routing, zero-write behavior, approval blocking, output structure, and unsupported-capability fallback.
