# Generic Agent Skills Adapter

`manifest.json` exposes canonical workflow metadata and contract paths without assuming a particular assistant runtime. This is an L1 parseability contract. Consumers must supply their own invocation and enforcement layer before claiming L2 or L3.
