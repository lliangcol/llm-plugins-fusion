# Minimal Public Consumer Fixture

This is a fictional, disposable consumer repository for adapter conformance. It contains no private project names, paths, endpoints, credentials, repository addresses, runtime flags, business rules, or knowledge-base content.

Use `request.md` as the routing input. The fixture is read-only: an adapter must select `review-only`, identify `REVIEW_SCOPE`, and avoid file writes or shell execution. This fixture does not install an assistant or prove live model quality.
